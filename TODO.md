# TODO
- [x] Inspect existing Dashboard.vue and Dashboard.backup.vue + DashboardLayout.vue
- [ ] Update `resources/js/Pages/Dashboard.vue` to contain exactly: one `<template>`, one `<script setup>`, and one `<style scoped>` (style after script)
- [ ] Ensure Dashboard.vue preserves: `tasks_proses`, `tasks_selesai`, `recent_tasks`, `todo_items`, `removeTodo()`, `statusStyle()`
- [ ] Ensure DashboardLayout import kept (do not delete)
- [ ] Fix Chart.js code: implement `salesAreaChart` and `orderBarChart` initialization in `renderCharts()`
- [x] Run `npm run build` to confirm Vite compiles
- [ ] Summarize changed files


